Create a simple test job to verify GitHub access permissions for the repository https://github.com/chadwyatt/ai-voicemail-netlify. The job should:

1. Clone the repository 
2. Check if we can read the files (list directory contents)
3. Create a small test file (like `test-access.txt` with a timestamp)
4. Attempt to commit and create a pull request
5. Report back on what permissions are available

This will be a minimal test to verify we can perform the basic operations needed for future work on your project.